---
// 이 부분은 웹사이트의 기능과 관련된 스크립트 영역입니다.
// 지금은 수정하지 않으셔도 괜찮습니다.
const personalInfo = {
  name: "김연우",
  school: "아스트로 고등학교",
  slogan: "여러분의 내일을 바꾸는 단 하나의 선택",
};

const promises = [
  {
    icon: "📚",
    title: "학습 환경 개선",
    description: "최신형 책걸상으로 교체하고, 모든 교실에 공기청정기를 설치하여 쾌적한 학습 분위기를 만들겠습니다.",
  },
  {
    icon: "🎨",
    title: "동아리 활동 지원 강화",
    description: "동아리 지원 예산을 20% 증액하고, 활동 공간을 확충하여 여러분의 꿈과 재능을 마음껏 펼칠 수 있도록 돕겠습니다.",
  },
  {
    icon: "🤝",
    title: "소통 채널 확대",
    description: "온라인 익명 건의함을 개설하고, 매달 '학생회와의 대화' 시간을 가져 여러분의 목소리에 항상 귀 기울이겠습니다.",
  },
  {
    icon: "🎉",
    title: "즐거운 학교 축제",
    description: "학생들이 직접 기획하고 참여하는 축제를 만들고, 유명 아티스트를 초청하여 모두가 함께 즐기는 축제의 장을 열겠습니다.",
  },
];
---

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{personalInfo.name} 후보 | {personalInfo.school} 학생회장 선거</title>
    <style>
        :root {
            --primary-color: #3b82f6;
            --secondary-color: #8b5cf6;
            --background-start: #1e3a8a;
            --background-end: #3b82f6;
            --card-background: rgba(255, 255, 255, 0.1);
            --text-color: #e0e7ff;
            --heading-color: #ffffff;
            --border-color: rgba(255, 255, 255, 0.2);
        }
        body {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, Roboto, 'Helvetica Neue', 'Segoe UI', 'Apple SD Gothic Neo', 'Noto Sans KR', 'Malgun Gothic', 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', sans-serif;
            margin: 0;
            padding: 0;
            color: var(--text-color);
            background: linear-gradient(135deg, var(--background-start), var(--background-end));
            overflow-x: hidden;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            position: relative;
            z-index: 1;
        }
        .hero {
            text-align: center;
            padding: 4rem 0;
            border-radius: 1rem;
            margin-bottom: 3rem;
            animation: fadeInDown 1s ease-out;
        }
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .hero h1 {
            font-size: 3.5rem;
            color: var(--heading-color);
            margin: 0;
            font-weight: 800;
            letter-spacing: -2px;
            background: linear-gradient(45deg, #a5b4fc, #e0e7ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .hero p {
            font-size: 1.5rem;
            color: var(--text-color);
            margin-top: 1rem;
            font-weight: 500;
        }
        .promises-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }
        .promise-card {
            background: var(--card-background);
            border: 1px solid var(--border-color);
            border-radius: 1rem;
            padding: 2rem;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.5s ease-out forwards;
        }
        .promise-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }
        .promise-card-header {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1rem;
        }
        .promise-icon {
            font-size: 2.5rem;
            line-height: 1;
        }
        .promise-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--heading-color);
        }
        .promise-description {
            font-size: 1rem;
            line-height: 1.6;
        }
        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* 각 카드가 순차적으로 나타나는 애니메이션 효과 */
        .promise-card:nth-child(1) { animation-delay: 0.1s; }
        .promise-card:nth-child(2) { animation-delay: 0.2s; }
        .promise-card:nth-child(3) { animation-delay: 0.3s; }
        .promise-card:nth-child(4) { animation-delay: 0.4s; }

        footer {
            text-align: center;
            padding: 2rem;
            margin-top: 4rem;
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.5);
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="hero">
            <h1>{personalInfo.name}</h1>
            <p>{personalInfo.slogan}</p>
        </header>

        <main>
            <div class="promises-grid">
                {promises.map(promise => (
                    <div class="promise-card">
                        <div class="promise-card-header">
                            <span class="promise-icon">{promise.icon}</span>
                            <h2 class="promise-title">{promise.title}</h2>
                        </div>
                        <p class="promise-description">{promise.description}</p>
                    </div>
                ))}
            </div>
        </main>
        
        <footer>
            © 2024 {personalInfo.name} Campaign. All rights reserved.
        </footer>
    </div>
</body>
</html>